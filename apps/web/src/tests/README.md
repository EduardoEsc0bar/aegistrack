# Frontend Test Structure

The initial mission-control scaffold focuses on runnable page structure and wiring.

Recommended first frontend tests:

- render dashboard KPI cards with mocked query responses
- render globe layer toggles and entity detail selection
- verify timeline view displays event severity labels
- verify settings page shows configured API and WebSocket endpoints
